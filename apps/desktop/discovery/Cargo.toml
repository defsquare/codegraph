[package]
name = "codegraph-discovery"
description = "Which codegraph extractors are installed on this machine, as the registry the daemon reads"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

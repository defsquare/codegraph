{
  "name": "@codegraph/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "codegraph command-line interface",
  "bin": {
    "codegraph": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@codegraph/analyzer": "workspace:*",
    "@codegraph/city": "workspace:*",
    "@codegraph/core": "workspace:*",
    "@codegraph/navigator": "workspace:*",
    "@codegraph/navigator-ui": "workspace:*",
    "@codegraph/viz": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}

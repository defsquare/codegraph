{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // A Vite app, not a Node library: bundler resolution, DOM available.
    // The model layer under src/model must still not touch the DOM — that
    // separation is by review and tests, the lib flag cannot express it.
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],
    "jsx": "react-jsx",
    "noEmit": true,
    "declaration": false,
    "declarationMap": false,
    "sourceMap": false
  },
  "include": ["src", "test", "vite.config.ts"]
}

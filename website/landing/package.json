{
  "name": "@codegraph/website-landing",
  "version": "0.1.0",
  "private": true,
  "description": "Codegraph landing page: Hugo, no theme, on the Defsquare Design System. Builds when the hugo binary is on PATH; skips (with a notice) otherwise, so Node-only environments stay green.",
  "scripts": {
    "build": "if command -v hugo >/dev/null 2>&1; then hugo --minify --gc; else echo 'hugo not installed: landing build skipped' >&2; fi",
    "dev": "hugo server --port 1315",
    "typecheck": "true",
    "test": "true"
  }
}

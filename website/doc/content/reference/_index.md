---
title: Reference
weight: 3
cascade:
  type: docs
---

The facts, stated once and completely: every command and option, the interchange format an extractor must emit, the metamodel's concepts, the shape of each artifact, the SQLite store, the environment variables. Nothing here teaches or argues; the [tutorials](/tutorials/) and [explanations](/explanation/) do that.

Where a generator exists, the page is its rendering: `codegraph <command> --help` for the CLI, `schemas/` for the interchange, `codegraph profiles --json` for the language profiles.
